import { {{pascalCase name}}Action, {{pascalCase name}}ActionTypes } from './{{pascalCase name}}ActionTypes'

export interface {{pascalCase name}}State {

} 

export const {{camelCase name}}InitialState:{{pascalCase name}}State = {

}

const {{camelCase name}}Reducer = (state:{{pascalCase name}}State,action:{{pascalCase name}}Action) => {
  switch(action.type){ 
//--AppendCase
    default: return state; 
  } 
} 

export default {{camelCase name}}Reducer;