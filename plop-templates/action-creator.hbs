export const {{camelCase name}} = ({{#if payloadName}}{{camelCase payloadName}}: {{payloadType}}{{/if}}): {{pascalCase store}}Action => ({
    type: {{pascalCase store}}ActionTypes.{{constantCase name}},{{#if payloadName}} payload: { {{camelCase payloadName}} } {{/if}}
  }) 
//--AppendCreator